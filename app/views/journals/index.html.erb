<h2>Hi <%= @user.name %>, here are your journals: %></h2>

<ul>
    <%= @journals.each do |journal| %>
       <li><%= link_to journal.horoscope.title, journal_path(journal) %></li>
    <% end %>
</ul>    